#### 左侧树形导航
要点：使用`column`分割左右
```php
$row = $builder->row();
$left = $row->column(2);
$right = $row->column(10);

$treeData = $thhis->getTree();//以某种方式获取的数据 
$this->assign('treeData', $treeData);
$left->content()->fetch('tree');//渲染一个自定义模板，在其中完成树形结构
//$left->content()->display($html);//或者通过拼接html的形式

$table = $right->table();

//略

```